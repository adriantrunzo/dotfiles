- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
      force: false
    ~/.config:
      recursive: true

- link:
    ~/.config/kitty/kitty.conf: kitty/kitty.conf
    ~/.config/nvim/init.lua: nvim/init.lua
    ~/.gitconfig: git/.gitconfig
    ~/.zshrc: zsh/.zshrc

